<%= form_for current_user.relationships.build(:friend_id => @user.id),
             :remote => true do |f| %>
    <div><%= f.hidden_field :friend_id %></div>
    <div class="actions"><%= f.submit "Add to friends" %></div>
<% end %>