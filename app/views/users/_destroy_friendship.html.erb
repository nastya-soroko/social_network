<%= form_for current_user.relationships.find_by_friend_id(@user),
             :html => { :method => :delete },
             :remote=> true do |f| %>
    <div class="actions"><%= f.submit "Delete from friends" %></div>
<% end %>
